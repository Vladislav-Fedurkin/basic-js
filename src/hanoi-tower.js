// const CustomError = require("../extensions/custom-error");

// module.exports = function calculateHanoi(disksNumber, turnsSpeed) {
//   turns = 0;
//   calculate(disksNumber);

//   let sec = turnsSpeed / 60 / 60;
//   return {turns:turns, seconds: Math.floor(turns /sec)}
// };

// let turns = 0;

// function calculate(n) {
//   if (n <= 0) {
//       return
//   }
//   calculate(n - 1);
//    turns++
//   calculate(n - 1);
// }